name: Test
on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x, 15.x, 16.x]

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        env:
          MONGODB_USERNAME: ${{ secrets.test.MONGODB_USERNAME }}
          MONGODB_PASSWORD: ${{ secrets.test.MONGODB_PASSWORD }}
          MONGODB_DATABASE: ${{ secrets.test.MONGODB_DATABASE }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
      - run: env
      - run: yarn install
      - run: npm run test-ci
